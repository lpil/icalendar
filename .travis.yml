language: elixir
elixir:
  - 1.9
  - 1.10
  - 1.11

otp_release:
  - 21.0
  - 22.0
  - 23.0

env: MIX_ENV=test

sudo: false # faster builds

notifications:
  email: false

script:
  - mix compile --warnings-as-errors
  - mix format --check-formatted --dry-run
  - mix test
  - mix docs
